# The environment used by Heroku Review Apps, declared in app.json as
# RAILS_ENV=review. It behaves exactly like production; only the database
# it connects to differs (see the review block in config/database.yml).
require_relative "production"
